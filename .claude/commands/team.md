---
description: Route a request to the right team member, tier, and workflow
argument-hint: [what you want to do]
---

Route this request using the `using-agent-dev-team` skill: $ARGUMENTS

Do all four steps and show your work in one short block before doing anything else:

1. **Class** — trivial, bounded, feature, architectural, or incident. When two fit, take
   the heavier one.
2. **Role** — the lowest tier from the roster that clears the ceiling. Say why that tier
   and not the one below.
3. **Skill** — the workflow to follow.
4. **First action** — the single next step.

Then stop and let me confirm or override before you start. If the request is an incident,
skip the confirmation and go straight to `incident-response`.
